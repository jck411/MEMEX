# vault

Markdown wiki output.

The wiki is generated and maintained from reviewed facts, but the markdown
itself is the user-facing product.

Vault contents may contain private information and are ignored by Git by
default. This README is only a placeholder so the expected local directory is
visible in a fresh clone.
